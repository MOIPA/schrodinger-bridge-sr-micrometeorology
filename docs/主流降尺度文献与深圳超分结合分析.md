# 导师推荐的 8 篇论文,和我们工作有什么关系(通俗版)

> 2026-09-05。导师觉得我们的实验"太乱",发了 8 篇论文(重点 4 篇)。下面用大白话讲:这些论文在做什么、和我们有什么关系、导师想让我们学什么、我们接下来该做什么。摘要原文在 `~/文档/tzq_work/paper/论文总结/`。

---

## 一、先记住三句话

1. **这 8 篇论文做的和我们是一件事**:都是"用粗网格气象场,让神经网络学会重建细网格场"。只是他们大多从 25~30 km 降到 2~3 km(跨度很大),我们是从 3 km 降到 1 km(跨度小但做的是三维多层风场)。所以他们的经验基本都能用到我们身上。
2. **我们有一个他们都没有的宝贝条件**:我们的 3 km 场和 1 km 场是**同一个 WRF 模拟跑出来的**(嵌套域),差别只有分辨率。别的论文里低分辨率(如全球再分析)和高分辨率(区域模式)是**两套完全不同的系统**,混着各种说不清的差别。只有我们能干干净净回答"分辨率不够到底丢了什么、能不能补回来"。
3. **导师说"乱",不是数据或模型有问题,而是我们的对比缺了"参照物"**——后面第三节细说。

---

## 二、8 篇论文与我们的横向对比(⭐ 是导师重点)

> 先看第一行"我们"作为参照。技术细节摘自 `paper/论文总结/` 各篇。

| 论文 | 用的模型(技术细节) | 输入(变量) | 输入精度 | 输出 | 和我们什么关系 |
|---|---|---|---|---|---|
| **我们** | **SI 扩散框架**(逐时间步生成)+ UNet 主干(1900 万参数),输出漂移场 b;物理约束:散度 ∇·b=0(权重 0.1)+ 大尺度涡度 ζ(b)=0(权重 0.05);垂直风 W 通道加权 ×10 | 3 km 风 U/V/W×6 层(约 10~300 m)+ 8 个条件(t2、地形 z、土地利用、地表温度、感热/潜热通量、地表气压、边界层高度) | **3 km**(模拟抹粗 或 原生 d03) | **1 km** 风 U/V/W×6 层 | —— |
| ⭐① Benton 2025(美国 NREL) | **三级级联 GAN**(3D 卷积):空间 30→10→2 km 两级 + 时间 2 km 逐时→5 分钟一级;损失 = 对抗 + MAE + **空间/时间最大最小值 4 项**(保极值);输入侧逐月矩匹配偏差校正;1 km 地形在网络中层注入 | 全球再分析 ERA5 的风(多高度)+ 温湿/通量指标 + 地形,约 13 类 | **30 km** 逐小时 | **2 km**、5 分钟风场(7 个轮毂高度) | 同样的"真实低精度配对",它多了观测塔验证和分布类评价 |
| ⭐② Ericson 2025(瑞士) | **9 种判别式回归模型横评**(SRCNN/SRResNet/U-Net 系/ViT/Uformer/自研 Windflow-SRnet);为对付强风欠预测做"普通分支 + 高风专家分支 + 融合"(非对称损失、标签重缩放);LR 插值到 1 km 后网络只学修正 | ECMWF 全球预报的风(7 层)、温度、湿度、气压、地形等,约 38 通道 | **9 km** | **1 km** 地面(10 m)风 u/v | 设定和我们几乎一样;示范"科学的对比实验"该怎么做 |
| ⭐③ Mardani 2025 CorrDiff(NVIDIA) | **两段式扩散**:① 回归 UNet 先学条件均值 μ;② 条件扩散(EDM 表述、像素空间、约 12 步采样)只对残差 r=x−μ 建模——大尺度结构(台风、锋面)由①保证,小尺度细节和尾部由②生成 | 全球再分析 ERA5 的风/温/水汽等 12 通道 | **25 km** | **2 km** 的 2 m 气温、10 m 风、雷达回波 | 扩散主流范式 + 科学评价体系(CRPS/谱/分布)出自它;敲打我们只报误差一个数 |
| ⭐④ Sun 2026(中国气象局) | **CorrDiff 扩展**(同上两段式)+ 新增**全局残差连接**:网络学 y−插值输入(粗尺度信息恒等跳过网络);20 成员集合;按层 min-max 归一化、小 batch | CMA-GFS 全球预报多变量 + 3 km 地形 | **25 km** | **3 km** 地表场 + 6 个气压层三维场 + 雷达回波 | 把扩散范式搬到中国、做高空多层;精度超过业务区域模式 CMA-MESO |
| ② Dettling 2025(NCAR) | **复合 ESRGAN**(两个 GAN 级联:4× + 8×);预热阶段只训生成器(MAE)→ 再上对抗(相对论损失 RaGAN);30 m 地形经 space-to-depth(像素重排成通道)无损注入;逐垂直层 2D 处理 | 约 1 km 的风 u/v/w + 温度 + **30 m 高分辨率地形** | 960 m | **30 m** 湍流风场(含垂直风) | 判断"细节真不真"要用能量谱和统计分布,而非单点误差 |
| ④ Kurihanna 2023(短文) | **SRGAN 式 GAN + 自研垂直自注意力 PWA**(沿垂直层轴做 8×8 注意力,水平方向仍用 CNN);预训练期对注意力图施加"锐化正则"(物理约束落在网络内部表示上)+ Sobel 梯度损失;对抗阶段补高频 | 风 U/V/W × 8 层(自己把 100 m 抹粗成 900 m) | 900 m | **100 m** 风 U/V/W × 8 层 | 三维含垂直风,与我们最像;论文较初步、无完整实验 |
| ⑦ Wu 2025(上海交大) | **专家库路由 SoZoGAN**:用单一公开数据预训练一组按"泰勒微尺度"(湍流的特征尺度)索引的 SRGAN,靠裁剪-缩放-重贴做尺度自增广;推理时把流场分区(谱聚类)、估计当地微尺度再选最匹配专家;散度软约束,**自适应权重**(物理项占比动态保持 >50%) | 湍流速度场 u/v/w(把高分辨率自己抹粗 5~16 倍) | 网格边抹粗 5~16 倍 | 还原的高分辨率湍流场 | 数据少也能泛化的配方;物理约束用自适应权重 → 针对我们"约束训练后期震荡"的改进思路 |
| ⑧ Zhou 2026(港科大广州) | 数据集 + 基准论文(附多任务方法横评);与超分最相关的任务③结论:**Kalman 同化 + POD 降阶先验**的误差只有深度模型的一半(深度模型在样本少时退化成预测均值场) | 屋顶 + 街道的 **16~256 个风观测点**(任务③) | 稀疏测点 | 全三维风场重建(40 m→10 m 网格) | 教训:样本极少时,用数据集自带的低维先验比硬上深度模型靠谱(呼应我们 d03 小样本) |

> 一眼看出的规律:① 技术路线上,GAN(①/②/④/⑦)、直接回归(②/③的均值部分)、扩散(我们/③/④)三分天下,其中③④的"先回归后扩散"已是当前主流——我们用的是端到端扩散(没有回归先验那一步);② 输入输出都是"模式风场 + 地面/地形条件",但只有我们和④/⑦做**垂直多层含 W** 的风场,其余多是地面一层风;③ 多数论文的粗/细场来自**两套不同模式系统**,只有我们是同一个 WRF 模拟的 3 km/1 km 嵌套输出。

### 追问:这些论文到底用没用物理约束?(很多人会问)

**结论:基本没用——它们确实是"纯超分",物理性靠两条腿撑着,而不是在损失里加方程。**

| 论文 | 训练损失里有没有物理方程项 | 那"物理"体现在哪 |
|---|---|---|
| ⭐① Benton | 无 | 事后检查物理统计量(能量谱、极值、日循环),找观测塔验证 |
| ⭐② Ericson | 无 | 输入里塞满物理量(风/温/湿/气压/地形),让网络自己学 |
| ⭐③ Mardani CorrDiff | 无 | 均值回归网络保住大尺度结构;谱/PDF 检查细节是否"物理地真" |
| ⭐④ Sun | 无 | 同上,落地为业务预报对比 |
| ② Dettling | 无 | 用矩、分布、能量谱检查生成的湍流"统计上像 LES" |
| ④ Kurihanna | 假物理:约束的是**注意力图锐化**(诱导它学垂直对流),不是方程残差 | 标题带 physics-informed,实为结构先验软正则 |
| ⑦ Wu | **有**:散度(连续性方程)软约束,自适应权重让物理项占损失一半以上 | 少有的真用者;但只用运动学约束 |
| ⑧ Zhou | 基准任务里带一个 PINN-UNet 对照(连续性残差),谱误差最低 | 说明物理项更多起"正则"作用而非提精度 |
| **我们** | **有**:散度 + 涡度,且做了同架构"有/无"严格消融 | 主流里少见的做法——这是我们的差异化标签 |

**主流为什么不加物理约束,我们要心里有数:**
1. **真值本身是模式输出**,统计规律里已经带着物理,学映射时误差项已经很小,约束带来的额外增益有限;
2. **工程上难且不稳**——我们自己的实验就是证据(固定权重训练后期震荡、中途模型最好),Wu 那篇也是用自适应权重才稳;
3. 他们用"**事后检查**"(谱、分布、观测)代替"训练时约束",成本低得多。

**对我们的三点含义:**
1. **物理约束是我们的特色,不是主流标配**——论文卖点定位应是"在真实低精度场景下系统检验运动学约束的价值",而不是"我们比主流多了约束所以更准";
2. **我们的证据要跟上**:物理约束目前拿得出手的证据是跨域鲁棒(phys 退化幅度 +22% vs baseline +61%)这类"约束带来稳健性"的结果,而不是绝对 RMSE;将来再加散度残差、谱这类"约束确实生效"的直接检查(报告下一步已有计划);
3. **全空位机会:完整 NS 动量残差约束 8 篇里没人做**(都只到散度为止),而深圳数据有算动量方程各项所需的全部变量——这是我们有条件做出别人做不了的工作的地方。

---

## 三、导师为什么说我们"乱",想让我们学什么

她重点发的 4 篇,其实在教我们一套**"怎么证明模型有用"的规范实验**,对照下来我们缺了三种"参照物":

### 缺参照物 1:没算"什么都不学"的底线
所有论文都比了一个最低标准:**不训练任何模型,直接把粗场插值放大当结果**,误差是多少。我们从来没算过。
> 打个比方:光说"我们模型误差 0.15",别人不知道这是好是坏;但如果说"啥也不学只插值是 0.6,我们学到了 0.15",就有意义了。
> ✅ 这个数字 47 号脚本正在补(你手机跑一下 `bash ops/queue/47_zero_model_baseline.sh` 就行)。

### 缺参照物 2:没跟"简单方法"比
Ericson 那篇专门问:**不用花哨的生成模型,直接用普通神经网络一步算出来,够不够好?** 我们所有模型都是同一个复杂框架(扩散),从没比过"不用扩散行不行"。
> 如果普通方法差不多,就要解释我们的复杂方法值在哪(比如细节更真);如果差,更要讲清楚。这个对比审稿人必问,现在缺着。

### 缺参照物 3:只看"误差大小"一种标准
8 篇论文评判"好不好"普遍用**三套尺子**,我们只有一把:
- 尺子 1:**误差大小**(RMSE 等)——我们有了
- 尺子 2:**分情况看误差**——强风段表现如何?每层高度表现如何?(我们只分了 U/V/W,没分强风、没分层)
- 尺子 3:**小尺度结构真不真**——能量谱(哪个尺度范围的波动被还原了)、概率分布。因为"误差小"和"看起来真"经常不是一回事:有的模型误差很小但场是"抹平"的,小尺度细节全丢了,风对风机/无人机/污染物扩散才是真没用的

另外扩散模型本该有的第四把尺子:**多次生成的结果差多少**(可信度),我们也从没用过——等于买了好车只开一档。

### 那 9 个模型怎么办:不是白做,是没摆好
我们的 9 个实验其实覆盖了三个有用的对照方向,只是混在一起讲才显得乱:
1. **模拟低精度 vs 真实低精度**(d04 训练 vs d03 训练、跨域测试)——**这个方向别人都没做过**,是我们的独门;
2. **物理约束有没有**(phys vs baseline 等)——规范做法;
3. **条件变量精度**(1km 条件 vs 3km 条件,allLR 系列)。

汇报/写作时把实验按这三个方向排成整齐的对照(一张矩阵表),而不是流水账列 9 个模型,就不乱了。昼夜分训(day/night)建议降级为补充说明,它最像"随手加的变量"。

---

## 四、接下来做什么(按省事程度排)

| 优先级 | 做什么(大白话) | 回答什么问题 | 要花什么 |
|---|---|---|---|
| ① | **算"零模型"底线**:直接把 3km 场当 1km 用,看误差 | 我们的模型比"不干活"好多少? | 47 号脚本,手机跑一次,几分钟 |
| ① | **把误差按强风段、按高度层拆开看** | 模型是不是"平时行、刮大风就废"?哪层最差? | 改评估统计,CPU 即可 |
| ① | **画能量谱**:预测场、真值场、输入场各画一条 | 小尺度结构真被还原了吗,还是被抹平了? | 新评估脚本,CPU 即可 |
| ① | **跨方案测试:myj 训练的模型拿去测 ysu 数据**(两套 PBL 方案互测) | 模型换个"陌生模式"还行不行?(我们只有一个月数据,这是现成唯一的泛化测试) | 改数据划分,几乎零成本 |
| ② | **补一个"直接一步预测"的普通网络**(不用扩散)做对照 | 我们这么复杂的框架,比简单方法好在哪? | 一次训练(和现有训练同样量级) |
| ② | **扩散模型多次生成,看结果差异**:同一输入生成 10 次,统计差异、报可信度 | 扩散模型"能提供不确定性"的价值能否兑现 | 评估脚本扩展,不需新训练 |
| ② | **消融条件变量**:逐个去掉 8 个条件变量看谁重要 | 哪些条件真正有用?(预期地形最重要)训练/部署时可裁剪 | 推理脚本扩展 |
| ③ | **改进物理约束训练**:权重自适应(约束太强时自动放松)或分两阶段 | 解决我们"物理模型训练后期震荡、中途最好"的毛病——这本身就能写成一条发现 | 训练代码小改 + 1 次训练 |
| ③ | 找观测数据(自动站/风廓线)验证 | 模式对模式的自证,说服力有限 | 先确认数据可得性 |

> 其中 ③ 的两条是"论文冲刺期"的;①②做出来,下次见导师就能讲一个"有参照、有规范、有卖点"的故事了。

---

## 五、我们最终能讲的故事(一段话)

> 同一段 WRF 模拟的 3 km 与 1 km 嵌套输出,构成了研究"真实低分辨率"最干净的数据条件——不像以往研究必须用两套不同模式拼凑。我们在此条件下系统对照了三种问题:**(1) 用模拟抹粗的数据训练的模型,遇到真实 3 km 场会差多少(垂直风最敏感,误差放大 22~61%);(2) 直接用真实 3 km 重训能不能追回来(不能——1 km 细节在源头就不存在,信息不可逆);(3) 物理约束和简单模型在这个任务上到底带来什么**。所有结论都有三类证据:误差指标、分情况(强风/分层/昼夜)表现、谱与分布诊断。

其中 (1)(2) 别人没做过,是我们的新东西;(3) 需要补齐上面的①(参照物)才能成立。

---

> 如果想知道某篇论文更细的内容,看 `paper/论文总结/` 对应文件(那份写得比较专业,当字典查就行)。完整原始 PDF 在 `paper/` 目录。
